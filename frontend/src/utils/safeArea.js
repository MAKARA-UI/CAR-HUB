export const SAFE_AREA_EDGES = ['top', 'bottom'];
export const SAFE_SCROLL_PADDING_BOTTOM = 32;
export const SAFE_ACTION_PADDING = 16;

export const getSafeActionPaddingBottom = (bottomInset = 0) => bottomInset + SAFE_ACTION_PADDING;
